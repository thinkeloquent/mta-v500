{
  "name": "@internal/fastify-code-repository-listing",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "fastify-apps/code_repository_listing/src",
  "projectType": "application",
  "tags": [
    "type:app",
    "scope:fastify"
  ],
  "implicitDependencies": [
    "@internal/skeleton"
  ],
  "targets": {
    "lint": {},
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run app:dev",
        "cwd": "fastify-apps/code_repository_listing"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run app:start",
        "cwd": "fastify-apps/code_repository_listing"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run test",
        "cwd": "fastify-apps/code_repository_listing"
      }
    }
  }
}
