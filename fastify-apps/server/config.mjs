/**
 * Server Configuration
 * Re-exports configuration from @internal/fastify-main for consistency.
 */

export {
  settings,
  getSettings,
  Settings,
  default,
} from "@internal/fastify-main/config.mjs";
