# NPM Registry Configuration
registry=https://registry.npmjs.org/

# @internal packages are local workspace packages - resolved via pnpm-workspace.yaml
# No registry needed for @internal scope

# Authentication tokens - uncomment and set when using private registry
# For npmjs.org (uncomment if publishing)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# For private registries - add your registry URL here
# //your-private-registry.com/:_authToken=${NPM_TOKEN}

# Prefer offline cache when available
prefer-offline=true

# Engine strict mode
engine-strict=false

# optional=false
