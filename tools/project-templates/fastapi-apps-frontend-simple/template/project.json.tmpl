{
  "name": "@internal/{{APP_NAME}}-frontend",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "fastapi-apps/{{APP_NAME}}/frontend/src",
  "projectType": "application",
  "tags": [
    "type:frontend",
    "scope:fastapi"
  ],
  "targets": {
    "lint": {},
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build",
        "cwd": "fastapi-apps/{{APP_NAME}}/frontend"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev",
        "cwd": "fastapi-apps/{{APP_NAME}}/frontend"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run test",
        "cwd": "fastapi-apps/{{APP_NAME}}/frontend"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run preview",
        "cwd": "fastapi-apps/{{APP_NAME}}/frontend"
      }
    }
  }
}
