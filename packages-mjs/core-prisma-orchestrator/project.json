{
  "name": "@internal/core-prisma-orchestrator",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages-mjs/core-prisma-orchestrator/src",
  "projectType": "library",
  "targets": {
    "lint": {},
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "tsc",
        "cwd": "packages-mjs/core-prisma-orchestrator"
      }
    },
    "build:watch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --watch",
        "cwd": "packages-mjs/core-prisma-orchestrator"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist",
        "cwd": "packages-mjs/core-prisma-orchestrator"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages-mjs/core-prisma-orchestrator"
      }
    }
  },
  "tags": [
    "type:library",
    "scope:core",
    "level:0"
  ]
}
