{
  "name": "@internal/config-loader",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages-mjs/config-loader/src",
  "projectType": "library",
  "tags": [
    "scope:shared",
    "type:lib"
  ],
  "targets": {
    "lint": {},
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages-mjs/config-loader"
      },
      "outputs": [
        "{projectRoot}/dist"
      ]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev",
        "cwd": "packages-mjs/config-loader"
      }
    }
  }
}
